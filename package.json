{
  "name": "school-agent",
  "version": "0.1.0",
  "private": true,
  "description": "School Agent - AI-powered educational assistant platform",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
