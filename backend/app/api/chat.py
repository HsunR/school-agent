"""Chat API router with SSE streaming endpoint."""

import json
import logging
from collections.abc import AsyncGenerator
from typing import Optional

from fastapi import APIRouter
from fastapi.responses import StreamingResponse

from app.core.settings import get_settings
from app.schemas.chat import ChatRequest
from app.services.chat_service import ChatService

from langsmith import traceable

logger = logging.getLogger(__name__)

router = APIRouter(prefix="/api/chat", tags=["chat"])

# Lazy-initialised singleton — avoids import-time dependency on API keys
chat_service: Optional[ChatService] = None


async def event_generator(
    messages: list,  # noqa: ANN401 — FastAPI dependency injection
) -> AsyncGenerator[str, None]:
    """Yield SSE-formatted tokens from the chat service.

    Args:
        messages: List of ChatMessage dicts from the validated request.

    Yields:
        SSE ``data:`` lines, one per token plus a final done marker.
    """
    global chat_service
    if chat_service is None:
        chat_service = ChatService(get_settings())

    try:
        async for token in chat_service.stream_chat(messages):
            yield f"data: {json.dumps({'token': token, 'done': False})}\n\n"
        yield f"data: {json.dumps({'token': '', 'done': True})}\n\n"
    except Exception:
        logger.exception("Error during SSE stream")
        yield f"data: {json.dumps({'error': 'Internal server error', 'done': True})}\n\n"

@traceable(name="chat_endpoint")
@router.post("")
async def chat_endpoint(request: ChatRequest) -> StreamingResponse:
    """Stream a chat response via Server-Sent Events.

    Accepts a ``ChatRequest`` JSON body and returns an SSE stream of response
    tokens generated by the ``ChatService``.

    **SSE format** (per ``docs/api.md``):

    .. code-block::

        data: {"token": "Hello", "done": false}\\n\\n
        data: {"token": " world", "done": false}\\n\\n
        data: {"token": "", "done": true}\\n\\n

    On errors the stream produces a single error chunk:

    .. code-block::

        data: {"error": "...", "done": true}\\n\\n
    """
    return StreamingResponse(
        event_generator(request.messages),
        media_type="text/event-stream",
    )


@router.get("")
async def chat_root() -> dict:
    """Chat root - placeholder response."""
    return {"message": "Chat API placeholder"}
